m1 = float(input('qual a primeira nota do aluno: '))
m2 = float(input('qual a segunda nota do aluno: '))
media = (m1 + m2) / 2
print(f'a primeira nota {m1:.2f} a segunda nota {m2:.2f} da a media de {media:.2f}')